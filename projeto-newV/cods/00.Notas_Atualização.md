# Resumo da Implementação

## 📁 Arquivos alterados / adicionados

### Modificados
- **player.h**  
  - A assinatura de `init_player` agora recebe o nome do jogador.

- **player.c**  
  - O nome do jogador passou a ser armazenado na struct `Player`.

- **main.c**  
  - Integração da **B+ tree**.  
  - Atualização das estatísticas durante o jogo.  
  - Salvamento dos dados ao sair do jogo.

### Adicionados
- **bptree.h**  
  - API da B+ tree e definição da struct `Stats`.

- **bptree.c**  
  - Implementação simples da B+ tree:
    - Criação
    - Inserção / atualização
    - Busca
    - Salvamento em arquivo
    - Liberação de memória

---

## 🧠 O que foi implementado

- Foi adicionada uma **B+ tree em memória** que indexa os jogadores pelo **nome**.
- Cada jogador possui um registro `Stats` contendo:
  - Vida
  - Vitórias
  - Quantidade de movimentos
  - Dano causado

### Fluxo de funcionamento
- No início do jogo, os **dois jogadores** são inseridos na árvore.
- Durante o loop do jogo:
  - Cada movimento incrementa `cont_mov`.
  - As estatísticas são atualizadas na B+ tree.
- Ao pressionar **`q`**:
  - A árvore é salva no arquivo `ranking.txt`.
  - A memória da árvore é liberada corretamente.

---

## ⚙️ Como compilar

Execute no diretório do projeto:

```bash
gcc *.c -o jogo -lncursesw
